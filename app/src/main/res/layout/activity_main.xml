<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:screenOrientation="landscape"
    android:background="@drawable/background"

    tools:context=".MainActivity">

    <Button
        android:id="@+id/btnONOFF"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentTop="true"
        android:layout_alignStart="@+id/btnFindUnpairedDevices"

        android:layout_marginTop="12dp"
        android:drawableLeft="@drawable/onoff"
        android:text="  BT Aç/Kapat" />

    <Button
        android:id="@+id/btnDiscoverable_on_off"
        android:layout_width="254dp"
        android:layout_height="wrap_content"
        android:layout_alignParentEnd="true"
        android:layout_alignTop="@+id/btnONOFF"
        android:layout_marginEnd="72dp"

        android:drawableLeft="@drawable/gorunur"
        android:onClick="btnEnableDisable_Discoverable"
        android:text="Cihazı Görünür Yap" />

    <Button
        android:id="@+id/btnFindUnpairedDevices"
        android:layout_width="494dp"
        android:layout_height="86dp"
        android:layout_alignParentStart="true"
        android:layout_alignParentTop="true"
        android:layout_marginStart="48dp"
        android:layout_marginTop="93dp"

        android:drawableLeft="@drawable/ara2"
        android:onClick="btnDiscover"
        android:text="Cihazları Ara"
        android:textStyle="italic" />

    <ListView
        android:id="@+id/lvNewDevices"
        android:layout_width="471dp"
        android:layout_height="155dp"
        android:layout_alignParentBottom="true"
        android:layout_alignParentStart="true"
        android:layout_marginStart="48dp"
        android:clickable="false"
        android:textColor="@android:color/background_light" />

    <Button
        android:id="@+id/btn_Home"
        android:layout_width="64dp"
        android:layout_height="wrap_content"
        android:layout_alignParentEnd="true"
        android:layout_alignTop="@+id/btnONOFF"
        android:background="@drawable/home" />


</RelativeLayout>